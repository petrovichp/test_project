# Copy to env.yaml and fill in real values.
# env.yaml is gitignored — never commit credentials.
DBHOST: "your-db-host"
DBUSER: "your-db-user"
DBPASSW: "your-db-password"
DBSQL: "your-db-name"
TOCKENONE: "BTC"
